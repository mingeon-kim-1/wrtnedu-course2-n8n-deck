# Slide 17c — API 키 받기 (3) — 받은 키는 URL의 crtfcKey 칸에 끼움

*신청 누르시면 보통 즉시 자동 승인이 뜹니다. 마이페이지 인증키 발급 현황에서 `crtfcKey` 라는 짧은 영문+숫자 문자열 하나를 복사해 메모장에 저장해 두세요. 이 키를 수업 중 n8n HTTP 블록의 URL 칸 `crtfcKey=` 다음에 그대로 붙여넣습니다. 여기까지 5분, 다음 슬라이드부터 본격 실습입니다. (약 90초)*

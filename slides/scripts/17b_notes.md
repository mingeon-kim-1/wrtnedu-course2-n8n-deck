# Slide 17b — API 키 받기 (2) — 활용 IP 칸은 형식만 맞으면 OK

*신청서에서 사용목적과 활용 IP를 물어봅니다. 사용목적 칸에는 "소상공인 정부지원금 알림 자동화" 라고 적으시면 됩니다. 활용 IP 칸은 본인 IP가 명확하지 않아도 괜찮아요 — `111.111.111.111` 처럼 형식만 맞으면 그대로 등록됩니다. 정부 API 신청서에서 자주 쓰는 표준 입력 방식이고, 실제 API 호출은 등록된 IP 외 네트워크에서도 정상 동작합니다. (약 90초)*

# Slide 17 — 오늘 API = K-Startup · 커리큘럼 '기업마당'도 같은 방식

*오늘 사용할 공고 API는 K-Startup입니다. 커리큘럼에는 '기업마당'으로 적혀 있는데, 두 사이트가 같은 공고를 거의 똑같이 싣기 때문에 수업에서는 K-Startup 을 씁니다. API 키 발급 없이 바로 호출 가능해서 시간을 아낄 수 있어요. 워크플로우 파일의 URL만 바꾸면 나중에 기업마당으로도 똑같이 동작합니다.*

# Slide 35 — 블록 5: 공고_매치_분기 (IF) + bizinfo 점수 예시

*블록 5번은 IF(분기). 'AI가 맞다고 판단한 공고만 다음으로 보내자'라는 스위치예요. 수업 중엔 '항상 참(1=1)'으로 두고 모든 공고가 다음 단계로 가게 합니다. 수업 후에는 AI가 준 점수가 0 이상인 공고만 통과하도록 바꾸시면 돼요 — 예를 들어 `[서울] + 외식업` hashtag 공고는 +2점으로 통과, `반도체`·`방산` hashtag 공고는 -3점으로 차단됩니다.*

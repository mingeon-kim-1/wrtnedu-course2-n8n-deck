# Slide 23 — 블록 2: 정부공고_API_호출 (HTTP)

*두 번째 블록은 정부공고_API_호출. 기업마당 서버에 "소상공인 공고 주세요"라고 요청을 보내는 블록이에요. URL 끝에 `hashtags=소상공인` 파라미터가 붙어 있어서 자동으로 필터링됩니다. 방금 받으신 API 키(crtfcKey)도 이 블록의 Query 파라미터에 연결돼 있어요. '실행해 보기' 누르시면 JSON이 20건씩 쏟아져 나옵니다.*
